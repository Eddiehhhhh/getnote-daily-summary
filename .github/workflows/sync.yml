name: Sync Daily Summary

on:
  schedule:
    # 北京时间凌晨 6:00 = UTC 22:00（前一天晚上）
    # 北京时间下午 13:00 = UTC 05:00
    # 每天跑两次，覆盖「昨晚22:00 ~ 今天中午」的所有录音
    - cron: '0 22 * * *'
    - cron: '0 5 * * *'
  workflow_dispatch:
  repository_dispatch:
    types: [sync]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install pynacl

      - name: Sync Get笔记 → AI分析 → Notion
        env:
          GETNOTE_API_KEY: ${{ secrets.GETNOTE_API_KEY }}
          GETNOTE_CLIENT_ID: ${{ secrets.GETNOTE_CLIENT_ID }}
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DB_ID: ${{ secrets.NOTION_DB_ID }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: python sync.py
